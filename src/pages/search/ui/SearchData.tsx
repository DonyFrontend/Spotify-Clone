import { ISearchData } from "../model/types/search-schema";

const SearchData = (data: ISearchData) => {
  console.log(data);

  return (
    <div className="p-3">
      
    </div>
  )
}

export default SearchData;